# Estado del proyecto

| Módulo            | Implementado | Validado end-to-end | Notas |
|--------------------|:---:|:---:|-------|
| ingest             | ✅ | ❌ | Implementado con ffmpeg/ffprobe vía subprocess (re-encode H.264/AAC, sin crop/rescale). Fuerza frame rate constante (`-fps_mode cfr` + `-r` con el `r_frame_rate` nominal detectado) para que detect_cuts pueda hacer cortes exactos por frame incluso si la grabación de OBS venía en VFR; si no se detecta un frame rate nominal fiable, cae a `-fps_mode cfr` sin `-r`. Validado con clips sintéticos (CFR 1920x1080@30fps y un caso VFR construido concatenando segmentos a 15/30/60fps) mediante la CLI real (`python -m src.ingest.run`); no probado aún contra una grabación real de OBS de 1-2h |
| transcribe         | ✅ | ❌ | Implementado con faster-whisper (CTranslate2), `word_timestamps=True`, iterando el generador de segmentos de faster-whisper sin chunking manual (ya es streaming) y logueando progreso periódicamente. Resuelve `device: auto` a cpu/cuda explícitamente y elige `compute_type` (float16 en cuda, int8 en cpu) al no haber esa clave en config. Descarta defensivamente palabras sin timestamp válido. Guarda `data/transcripts/<video_id>.json` y registra el estado en `data/pipeline.db` (nuevo `src/common/db.py`, SQLite). Validado end-to-end SOLO con el modelo `tiny` en CPU sobre un clip sintético de 5s sin voz (testsrc2+sine) generado a partir de la etapa de ingesta real; no probado con el modelo `medium` configurado ni con una grabación real con voz |
| detect_cuts        | ✅ | ❌ | Implementado: silencios vía energía RMS (librosa, umbral `silence_db_threshold` en dBFS), muletillas por coincidencia exacta de subsecuencia (normalizada) contra `transcript['words']`, y filtro de movimiento visual (optical flow denso Farneback) que usa el percentil 90 (no la media) de las magnitudes muestreadas en el tramo — necesario porque `detect_silence_segments` agrupa en un único tramo cualquier silencio de audio continuo, que puede mezclar quietud real con un momento de acción; promediar diluiría esa acción por debajo del umbral y el tramo se cortaría igual. Aplica `cut_margin_seconds` y fusiona cortes solapados tras el margen. Guarda `data/cuts/<video_id>/cuts.json` y registra `cuts_detected` en `data/pipeline.db`. Validado end-to-end con un clip sintético construido con ffmpeg (tramos alternando estático/movimiento y silencio/audio, más un transcript sintético con muletillas) a través de la CLI real; no probado con una grabación real de OBS. **Optimización de rendimiento (2026-08-04):** la primera versión llamaba a `score_motion_segment` de forma independiente por cada candidato, abriendo el vídeo y haciendo `cv2.VideoCapture.set(CAP_PROP_POS_FRAMES, ...)` (seek) en cada llamada; con un GOP largo (keyint por defecto de x264, ~250 frames) cada seek forzaba redecodificar desde el keyframe anterior, y con 137 candidatos sobre un vídeo de 9 minutos el filtro de movimiento tardó **~58 minutos**. Se sustituyó por `compute_motion_timeseries`, que recorre el vídeo una única vez de principio a fin sin seeks (usa `cap.grab()` para saltar los frames no muestreados) y guarda una serie temporal de magnitud de flujo óptico muestreada cada 0.3s; `score_motion_segment` ahora solo consulta esa serie ya calculada (percentil 90 en el rango del candidato) en vez de releer el vídeo. Verificado que el resultado (`cuts.json`) es idéntico al de antes de la optimización, reejecutando el mismo test sintético (13s, 4 candidatos). Medido además en un benchmark sintético a escala comparable al caso reportado (clip de ffmpeg de 9 min, 180 candidatos): **~62s** con la versión optimizada (~56x más rápido que los ~58 min anteriores, de "horas" a "segundos/minutos" como se pedía). **Fix de precisión en detect_silence_segments (2026-08-04):** revisión manual detectó un corte `silence` de 298.504s-302.232s que se comía el arranque de la frase de un compañero de stream que empezaba a hablar en el segundo 300. Causas encontradas: (1) `librosa.load(..., mono=True)` mezclaba los canales a mono antes de medir energía; si la voz de un compañero está paneada predominantemente a un canal (mic propio vs. audio de Discord en otro canal), la mezcla a mono diluye esa voz ~6dB y la mantiene por debajo de `silence_db_threshold` más tiempo del real — cambiado a analizar cada canal por separado y tomar el MÁXIMO de energía entre canales; (2) el umbral único `silence_db_threshold` no distingue silencio real de una voz que empieza floja y sube de volumen gradualmente, así que el candidato de silencio se extendía hasta que la voz cruzaba ese umbral — añadido un segundo umbral más estricto (`silence_onset_margin_db`, nueva clave en config, default 10dB) que recorta cada candidato a su núcleo de silencio "profundo", descartando la cola donde la energía ya está subiendo. Verificado con clips sintéticos (silencio real + rampa de voz floja de 2.5s, con y sin paneo a un solo canal): el candidato de silencio pasó de comerse 0.408-0.824s de la voz real (según paneo) a solo 0.120s, que el `cut_margin_seconds` (0.2s) existente ya absorbe por completo — el corte final tras el margen termina en 4.92s, ANTES del inicio real de la voz (5.0s). Confirmado además que silencios reales largos sin voz no se ven afectados (resultado idéntico antes/después del fix). De paso, se detectó y corrigió un riesgo relacionado en `ingest/run.py`: solo se mapea la pista de audio 0 (`-map 0:a:0`); si la grabación de OBS tiene varias pistas de audio (p.ej. una para el mic propio y otra para el audio de Discord de un compañero en un track separado), las demás se descartaban en silencio sin ningún aviso — ahora se loguea un WARNING explícito si se detecta más de una pista de audio en la ingesta. **Fiabilidad en grabaciones de 1-2h (2026-08-04):** tras un cuelgue real de >1h en `compute_motion_timeseries` sobre un vídeo de 1h39m sin actividad visible de CPU/disco, se investigaron causas conocidas de cuelgue silencioso de `cv2.VideoCapture` en Windows (backend Media Foundation con fugas/cuelgues documentados en grabaciones largas; también posible con el propio backend FFMPEG si su pool de hilos de decodificación se bloquea, o interferencia de antivirus/EDR en la lectura secuencial de archivos grandes — ninguna reproducible con certeza desde este entorno). Se añadió: (1) backend FFMPEG explícito (`cv2.CAP_FFMPEG`) en vez de fiarse de la auto-detección; (2) progreso visible (log cada 50 muestras o 30s de reloj, igual que `transcribe/run.py`); (3) checkpointing a `data/cuts/<video_id>/_motion_checkpoint.npz` cada ~10% del total estimado de muestras, validado contra mtime/tamaño del vídeo de entrada, que permite reanudar sin recalcular desde cero; (4) un watchdog que ejecuta el recorrido del vídeo en un hilo aparte y aborta con un error diagnóstico claro (en vez de colgarse en silencio) si no llega ninguna muestra nueva en 120s — un hilo bloqueado en una llamada nativa de cv2 no se puede matar de forma segura desde Python, así que lo único posible es dejar de esperarlo y fallar alto con el progreso ya guardado. Verificado con tests sintéticos monkeypatcheados: interrupción a mitad de cálculo + reanudación exitosa desde el checkpoint (encontrando y corrigiendo de paso un `PermissionError` de Windows real: `np.load()` sobre el `.npz` dejaba el archivo bloqueado si no se cerraba con `with`, y el siguiente intento de sobrescribir el checkpoint fallaba); cuelgue simulado detectado correctamente con el checkpoint previo intacto. Validado también con una ejecución completa real sobre `midnight_shift_test` (1920x1080, 9 min): terminó en ~498s con logs de progreso continuos y limpieza correcta del checkpoint al terminar — bastante más lento que el benchmark sintético anterior a 640x360 (~62s para un vídeo comparable), consistente con que Farneback escala con nº de píxeles (1080p tiene ~9x los píxeles de 640x360); para una grabación de 1-2h a esta resolución, extrapolando, el cálculo de movimiento por sí solo podría tardar del orden de 1-1.5h, lo que probablemente explica el cuelgue original reportado (aunque ahora sea diagnosticable y reanudable en vez de una espera silenciosa). **Optimización: reescalado antes de optical flow (2026-08-04):** cada frame se reescala a `_MOTION_ANALYSIS_HEIGHT_PX` (480p, preservando aspect ratio, sin upscale) antes de Farneback — el coste de Farneback escala con nº de píxeles y "hay movimiento sí/no" no necesita resolución completa. La magnitud se reescala de vuelta a equivalente-resolución-original (dividiendo por el mismo factor de escala) antes de guardarse, así que `_MOTION_NORM_MAGNITUDE_PX` no cambia. Verificado con clip sintético 1080p (movimiento claro vs. quietud total): clasificación idéntica con y sin reescalado, ~4.96x más rápido (cerca del ~5.06x teórico por área de píxeles, (1080/480)²). Sobre el vídeo real `midnight_shift_test` (9 min, 1080p): el cálculo de movimiento bajó de ~487s a ~103s (total del run: 106s vs. 498s), con 130 candidatos re-evaluados a ambas resoluciones para comparar: 7 de 130 (5.4%) cambian de clasificación, y los 7 cambian en la MISMA dirección segura — el reescalado rechaza (conserva) candidatos límite que a resolución completa se habrían aceptado (cortado), nunca al revés — consistente con la regla de CLAUDE.md de no cortar nunca movimiento real, a costa de ser algo menos agresivo cortando silencios ambiguos cerca del umbral (21→14 cortes reales en este vídeo, -5.16s de duración recortada). Extrapolando el ~4.8x de mejora total a una grabación de 1-2h, el cálculo de movimiento pasaría de ~1-1.5h estimadas a ~12-20 minutos | **Recorte automático de intro por detección de cara (2026-08-08):** nueva función `detect_intro_face_cut`, independiente de silencio+movimiento (se aplica siempre que se detecte con fiabilidad una intro sin cara, no hace falta que ese tramo también sea silencio). Muestrea `data/raw/<video_id>.mp4` cada `intro_face_sample_interval_seconds` (default 1.5s) con un recorrido secuencial de un solo paso (grab/retrieve, sin seeks — misma razón de rendimiento que `compute_motion_timeseries`), buscando la primera cara dentro de `facecam_region` (movido de `config['edit']` a la raíz de la config, compartido ahora con `edit/`). Para evitar falsos negativos puntuales exige que al menos `intro_face_min_detection_ratio` (default 70%) de las muestras dentro de una ventana de `intro_face_confirm_window_seconds` (default 8s) tengan cara detectada antes de confirmar (`_confirm_window_at`), devolviendo el instante de la PRIMERA muestra detectada dentro de esa ventana ya confirmada. Si no se confirma ninguna aparición en los primeros `intro_face_max_search_seconds` (default 15 min), no se recorta nada — evita cortar el vídeo entero por un fallo del detector — y se loguea un aviso. El corte resultante (0 hasta ese instante menos `cut_margin_seconds`, reutilizado) se añade en `run()` a `margined` antes de `_merge_overlapping_cuts`, como un cuarto tipo de corte (`type: "intro"`) que extiende el contrato de `cuts.json` documentado en CLAUDE.md (antes solo `silence`|`filler`). Configurable con `config['detect_cuts']['trim_intro']` (default true). **Detector de caras — cambio de plan durante la implementación:** la idea original (y la del usuario) era un Haar cascade clásico (`cv2.CascadeClassifier`), pero la versión de OpenCV instalada en este proyecto (`opencv-python` 5.0.0.93, la que resuelve el floor `>=4.9.0` de `requirements.txt`) eliminó ese binding de Python por completo (confirmado: sin `CascadeClassifier` ni ninguna constante `CASCADE_*` en `dir(cv2)`, pese a que `cv2.data.haarcascades` sigue existiendo). Consultado con el usuario, se optó por `cv2.FaceDetectorYN` ("YuNet", detector de caras basado en DNN que sí trae esta versión de OpenCV) en vez de fijar `opencv-python` a una versión 4.x; el modelo ONNX (~230KB, descargado de `opencv_zoo` vía el endpoint de media de GitHub LFS, ya que la URL raw normal solo sirve el puntero LFS de 131 bytes) se guarda en `assets/models/face_detection_yunet_2023mar.onnx`. Validado con un test sintético (`cv2.VideoWriter`, frames sin marcador los primeros 10s + frames con marcador brillante el resto, con un detector inyectado — parámetro `detector` de `detect_intro_face_cut` — que reconoce el marcador en vez de depender de que YuNet reconozca un marcador dibujado a mano): confirma correctamente en ~10.0s, tolera parpadeos puntuales dentro del tramo con cara (sigue confirmando), un blip aislado sin confirmación sostenida NO dispara el corte, `trim_intro=False` devuelve `None` sin abrir el vídeo, y un vídeo sin ninguna aparición de cara + `max_search` corto devuelve `None` con aviso en vez de recortar todo el vídeo. La lógica de ventana de confirmación (`_confirm_window_at`/`_confirm_intro_end_time`) se verificó además de forma aislada con listas de muestras sintéticas (sin detección, blip aislado, ratio insuficiente 50%, span insuficiente). El modelo YuNet real (detector por defecto, sin inyectar) se probó también contra un frame real del proyecto (`frame_horizontal.png`, 1920x1080) recortado con el `facecam_region` real de `settings.yaml`: detecta la cara correctamente dentro de esa región (confianza ~0.90) y NO detecta nada en una región vacía de control (esquina opuesta del mismo frame). **Validado contra el vídeo real `dinoblade_1` (2026-08-08):** con `intro_face_max_search_seconds` en su valor por defecto de entonces (900s/15min) no confirmó ninguna aparición — no por fallo del detector, sino porque la intro REAL de este vídeo (pantalla de carga estática + un tramo de webcam a PANTALLA COMPLETA antes de pasar a la disposición de juego normal) dura ~17-18 min, más que ese límite. Confirmado extrayendo frames manualmente con ffmpeg a varios instantes: t=30s-~850s pantalla de carga sin ninguna cara en pantalla; t=900s-~1000s webcam a pantalla completa (cara presente, pero FUERA de `facecam_region`, que apunta a la esquina donde va la webcam en la disposición de juego normal, no a pantalla completa); t=1100s ya con la disposición de juego normal y la cara dentro de `facecam_region`. Confirmado además, invocando directamente `_frame_has_face`/`_facecam_crop_box` sobre un frame real recortado a `facecam_region` (t=1200s, disposición de juego), que el detector SÍ reconoce esa cara correctamente — no era un problema del modelo ni del recorte, solo que la ventana de búsqueda por defecto se quedaba corta para la intro particular de este vídeo. Tras consultarlo con el usuario, se subió `intro_face_max_search_seconds` de 900s a 1200s (20 min) en `settings.yaml`; revalidado con el nuevo límite: `detect_intro_face_cut` confirma de forma fiable a partir de **1044.00s** (17.4 min). Reejecutado `detect_cuts.run` completo sobre `dinoblade_1` con este límite ya aplicado: 147 cortes, 1318.33s de duración total eliminada (139 silencio + 8 muletilla + el corte de intro 0.00s-1043.80s), y `edit.run` sobre ese `cuts.json` produjo un `final.mp4` de 4597.32s revalidado con `ffprobe` paquete a paquete (0 discontinuidades de PTS, ver la fila de `edit` para el detalle del mecanismo de corte). **Bug encontrado en ese mismo cuts.json y corregido (2026-08-08): el `type` del corte de intro se perdía al fusionarse.** `_merge_overlapping_cuts` fusiona candidatos solapados tras aplicar el margen (`cut_margin_seconds`), y el corte de intro (0.00s-1043.80s) casi siempre solapa con algún candidato de silencio real dentro de esa misma ventana (una intro larga suele contener silencios de verdad, además de la propia ausencia de cara) -- la regla de fusión anterior ("si el candidato entrante es `silence`, el `type` del grupo pasa a `silence` incondicionalmente") sobrescribía el corte de intro a `"type": "silence"` en cuanto se fusionaba con cualquier silencio solapado. El corte en sí seguía siendo correcto (mismo `start`/`end`, y el `reason` conservaba el texto "intro sin cara detectada..."), pero `cuts.json` ya no permitía distinguirlo de un silencio normal -- confirmado exactamente así en `dinoblade_1`: `{"start": 0.0, "end": 1043.8, "type": "silence", ...}` en vez de `"type": "intro"`. Fix: `_CUT_TYPE_PRIORITY = ["intro", "filler", "silence"]` sustituye la regla incondicional por una prioridad explícita (índice más bajo gana la fusión) -- `"intro"` nunca se sobrescribe, y entre `"filler"`/`"silence"` se invirtió el criterio anterior a favor de `"filler"` (una muletilla implica habla real, es la señal más específica de las dos). Verificado con `tests/test_merge_overlapping_cuts.py` (sin ffmpeg/vídeo, <1s): el caso reportado (intro+silence solapados, en ambos órdenes de entrada), intro+filler, filler+silence, una cadena de tres tipos solapando en serie (el resultado final es "intro" sin importar el orden de fusión), y un caso de control con cortes disjuntos (deben quedar separados, cada uno con su propio type). Regenerado `cuts.json` de `dinoblade_1` con el fix: mismos 147 cortes y 1318.33s (resultado determinista, solo cambian las etiquetas) pero ahora `{silence: 132, filler: 14, intro: 1}` -- 6 muletillas que antes también habían quedado enmascaradas como `silence` (mismo bug, caso filler+silence) ahora se etiquetan correctamente, y el corte 0.00s-1043.80s ya aparece como `"type": "intro"`. **Nota operativa:** la primera regeneración de este `cuts.json` se interrumpió sola a los ~35 min (el proceso de `detect_cuts.run` desapareció de la lista de procesos sin que este código lo provocara -- posible variante del cuelgue/interferencia de Windows ya documentado más arriba, "Fiabilidad en grabaciones de 1-2h"); el checkpoint de movimiento visual ya guardado (~85%) permitió reanudar y terminar en ~6 min en vez de recalcular desde cero, confirmando en la práctica que el mecanismo de checkpointing cumple su propósito ante interrupciones reales, no solo simuladas en tests. **Exclusión de facecam_region del cálculo de movimiento (2026-08-09, idea tomada de auto-editor de WyattBlue -- ver el docstring del módulo y de `_build_motion_exclusion_mask` para el detalle):** antes, `compute_motion_timeseries` medía la magnitud de cada muestra como la media espacial del frame COMPLETO (tras reescalar a 480p), así que el propio streamer moviéndose en su `facecam_region` (gestos, risas, hablar) contribuía a "hay movimiento en pantalla" y podía evitar que se cortara un silencio real sin acción en el contenido -- el caso contrario al que protege la regla de silencio+quietud de CLAUDE.md. Fix: nueva clave `config['detect_cuts']['exclude_facecam_from_motion']` (default true); si está activa, `_build_motion_exclusion_mask` construye una máscara booleana (del tamaño del frame de ANÁLISIS, con `facecam_region` reescalada proporcionalmente desde el frame original) y `_motion_worker` calcula la media de magnitud SOLO sobre los píxeles fuera de esa máscara -- excluir en vez de poner a cero manteniendo el mismo denominador, para no diluir la sensibilidad al movimiento real del resto del frame según lo grande que sea `facecam_region`. El checkpoint de movimiento visual (ver más arriba) ahora guarda también una huella de la máscara usada (`_motion_mask_signature`, hash de forma+contenido, o `"none"` si no hay máscara) y `_load_motion_checkpoint` la compara contra la de la ejecución actual antes de reanudar -- sin esto, cambiar `exclude_facecam_from_motion` o `facecam_region` entre una ejecución interrumpida y su reanudación mezclaría en una misma serie muestras calculadas con y sin exclusión, de forma silenciosa; el checkpoint se carga ahora DESPUÉS de conocer las dimensiones del vídeo (antes se cargaba antes de abrirlo), porque construir la máscara necesita esas dimensiones. Verificado con dos tests sintéticos: (a) `tests/test_motion_facecam_exclusion.py` -- genera con `cv2.VideoWriter` dos vídeos pequeños (320→420x200px, fondo con textura fija para que Farneback tenga gradiente real y no ruido de estimación sobre una superficie plana) con un bloque que rebota SOLO dentro de `facecam_region` o SOLO fuera, con margen de sobra respecto al borde de su contenedor (evita que el suavizado de Farneback "sangre" señal del objeto hacia la zona vecina); con el bloque moviéndose solo dentro de `facecam_region`, la puntuación de `score_motion_segment` baja de 0.368 (sin excluir -- por encima de `motion_threshold` 0.15, es decir, ANTES se habría descartado por error un silencio real) a 0.016 (excluyendo -- por debajo del umbral, correctamente "sin movimiento real"; no es exactamente 0 porque el códec con pérdida usado para el vídeo sintético, mp4v, no deja los frames "estáticos" bit-a-bit idénticos tras codificar/decodificar); con el bloque moviéndose fuera de `facecam_region`, la puntuación se mantiene por encima del umbral tanto excluyendo (0.476) como sin excluir (0.367) -- confirmando que excluir la webcam no oculta movimiento real del contenido (la diferencia entre ambos valores es el efecto matemático esperado de promediar sobre menos píxeles al excluir una región que aquí no aporta nada, no una señal escondida). (b) `tests/test_motion_checkpoint_mask_signature.py` -- confirma que un checkpoint se reanuda solo si la máscara coincide exactamente (misma región excluida, sin máscara en ambos casos) y se invalida en cualquier otra combinación (región distinta, con máscara vs. sin máscara en cualquiera de los dos sentidos). Pendiente de decidir con el usuario si merece la pena reprocesar `dinoblade_1` (o `icarus_1`) con este ajuste activo -- no se ha relanzado ningún vídeo real todavía, solo validado con sintéticos. **`facecam_region` pasa a ser fracción 0.0-1.0 del frame, no píxeles absolutos (2026-08-10):** encontrado al procesar `shift_at_midnight_1`, la primera grabación real a una resolución distinta de 1920x1080 (2560x1440) -- `facecam_region` se aplicaba como píxeles absolutos SIN reescalar según la resolución real del vídeo (confirmado leyendo `facecam_crop_box`/`_build_motion_exclusion_mask`: ningún camino de código comparaba la resolución del vídeo con la de referencia), así que la calibración de 1920x1080 (x:15,y:400,w:365,h:210) habría recortado una zona sin relación con la webcam real en un vídeo 2560x1440. Fix: nueva `facecam_region_to_pixels` (`src/common/face_detection.py`) convierte x/y/w/h como fracción 0.0-1.0 del frame a píxeles absolutos para el frame_width/frame_height reales de cada vídeo (con `round()`, no truncado, para que la conversión sea exacta); `facecam_crop_box` la usa internamente, y `_build_motion_exclusion_mask` (antes reescalaba manualmente por `analysis_height/orig_height`) se simplifica para aplicarla directamente a la resolución de análisis. `settings.yaml` se actualiza con la conversión EXACTA de la calibración validada en 1920x1080 (x:0.007813,y:0.370370,w:0.190104,h:0.194444) -- verificado que `facecam_region_to_pixels(esa_fraccion, 1920, 1080)` reproduce exactamente `{x:15,y:400,w:365,h:210}`, retrocompatibilidad exacta con `dinoblade_1`/`icarus_1`. Para `shift_at_midnight_1` en concreto se usó una región calibrada a mano por separado (no la fracción global derivada de 1920x1080, que habría quedado razonablemente cerca pero menos precisa): un frame real se inspeccionó visualmente (t=300s de la parte automática) y se verificó con un recorte de prueba que `x=40/2560,y=470/1440,w=500/2560,h=340/1440` encuadra la webcam completa con margen -- pasado como override en memoria a `detect_cuts.run`/`edit.run` para ese vídeo únicamente, sin tocar el default de `settings.yaml`. Tests actualizados para el nuevo contrato (fracción, no píxeles): `tests/test_motion_facecam_exclusion.py` (separa contenedores en píxeles para dibujar el vídeo sintético de la fracción que se pasa a la config) y `tests/scale_test_edit_pipeline.py`; ambos revalidados con el mismo resultado que antes del cambio. |
| detect_chapters    | ✅ | ✅ | Implementado: `detect_chapters_with_claude` analiza la transcripción completa con Claude (`config['detect_chapters']['claude_model']`, structured outputs vía `client.messages.parse`/`output_format`) y propone capítulos sobre la línea de tiempo ORIGINAL; `remap_chapters_to_edited_timeline` los ajusta al vídeo ya editado — si un timestamp propuesto cae dentro de un tramo cortado se ajusta primero al inicio del tramo conservado más cercano (`_snap_to_nearest_kept_start`, comparando distancia al `start` de ambos lados de forma consistente — bug encontrado y corregido antes de la primera ejecución real: la primera versión comparaba distancia al *final* del tramo anterior contra distancia al *inicio* del siguiente, una métrica inconsistente que podía saltar el timestamp hacia atrás más de lo esperado), y luego se remapea restando la duración acumulada de cortes anteriores (`src/common/timeline.py`, extraído de `edit/run.py` para que ambos módulos lo compartan sin importarse lógica de negocio entre sí). Aplica `min_chapter_seconds` como separación mínima sobre la línea de tiempo YA EDITADA (no la que ve Claude, que puede estar más separada de lo que queda tras los cortes), y garantiza que el primer capítulo quede en 0:00 (anteponiendo uno genérico "Introducción" si hace falta). Guarda `data/chapters/<video_id>/chapters.json` y `data/output/<video_id>/chapters.txt`, y registra `chapters_detected` en `data/pipeline.db`. Validado con `tests/test_detect_chapters.py` (cliente de Claude falso inyectado, sin red) y con una ejecución real completa contra `dinoblade_1`: 16 capítulos coherentes con el contenido real del directo, primer capítulo en 0:00, timestamps ya remapeados a la línea de tiempo editada (ver `data/output/dinoblade_1/chapters.txt`) |
| edit               | ✅ | ✅ | Implementado: `apply_cuts_with_zoom` corta los tramos de `cuts.json` y aplica el zoom en DOS pasos independientes (corte y zoom en `filter_complex` separados — ver **Rediseño en dos pasos (2026-08-05)** más abajo para el porqué y el diseño actual; el resto de este módulo no ha cambiado). `normalize_audio` aplica loudnorm en dos pasadas (mide + aplica, objetivo -14 LUFS/-1.5dBTP/11LU) con `-c:v copy` (no recodifica vídeo). `append_outro` concatena `assets/outro/outro.mp4` si existe: prueba primero una concatenación rápida sin recodificar (concat demuxer, `-c copy`), pero SOLO si ffprobe confirma que el outro tiene la misma resolución/fps/sample rate/canales que el clip — el concat demuxer con `-c copy` NO valida esto por su cuenta y, si no coinciden, no falla, produce un archivo "correcto" pero corrupto (fps mezclado, duración incorrecta); si no coinciden, recae en una recodificación vía `filter_complex` que normaliza el outro a los parámetros reales del clip (incluido su fps, no un valor fijo). Guarda `data/output/<video_id>/final.mp4` y registra `edited` en `data/pipeline.db`. **Zoom (rediseñado 2026-08-04, dos veces):** primero se implementó como "respiración de cámara" simétrica (subida y bajada suaves centradas en el frame); luego se rediseñó al efecto típico de streamer: durante los tramos de habla continua de `long_speech_min_seconds` segundos o más (default 10s, detectados igual que antes vía `detect_long_speech_segments` + remapeo a la línea de tiempo ya cortada — verificado con un caso real donde un corte de 0.422s cae dentro de un tramo de habla larga y el remapeo ajusta su duración exactamente en esa cantidad), el zoom sube lento y suave (curva coseno) hasta `long_speech_zoom_factor` (default 1.13) durante los primeros `zoom_in_duration_seconds` (default 2.5s) del tramo, dirigido hacia `facecam_region` (nueva clave de config: posición aproximada x/y/w/h de la webcam sobre el frame 1920x1080 original); SE QUEDA en ese zoom el resto del tramo (no vuelve a bajar gradualmente); y al terminar el tramo CORTA SECO a 1.0 (salto instantáneo). Implementación: `scale` (sí soporta expresiones con `t` vía `eval=frame`) agranda el frame entero, y `crop` (tamaño fijo, sin opción `eval` — sus w/h no admiten `t` en este build de ffmpeg, pero x/y sí) recorta una ventana del tamaño original cuya posición sigue al punto de la webcam ya escalado; a zoom=1.0 la ventana solo cabe en (0,0), o sea sin desplazamiento visible. Validado con un vídeo sintético con marcadores de color (verde en `facecam_region`, rojo en la esquina opuesta): el verde crece de 3.70% a 7.24% del frame y el rojo desaparece a 0% durante la rampa; ambos se mantienen EXACTAMENTE iguales (7.24-7.25%) durante toda la fase de sostenido; y en el frame siguiente al fin del tramo (1/30s después) vuelve de golpe a 3.70%/1.45% — confirmando rampa dirigida, sostenido sin decaimiento, y corte instantáneo sin frames de transición. **Ajuste de timing del zoom (2026-08-05):** el diseño de "sostenido durante el resto del tramo" resultó demasiado brusco/largo en la práctica, así que se quitó la fase de sostenido: `zoom_in_duration_seconds` sube de 2.5s a 4.5s por defecto (rampa más lenta), y `_build_facecam_zoom_expr` ya no ancla el corte seco al final del tramo de habla (`between(t,start,end)`) sino al final de la rampa (`between(t,start,start+ramp_seconds)`) — el zoom ya no se mantiene sostenido en absoluto, su duración visible total es siempre `ramp_seconds`. Si un tramo de habla dura menos que `ramp_seconds` no da tiempo a completar la rampa, así que se descarta sin aplicar zoom (antes se comprimía la rampa con `min(ramp_seconds, dur)` para que cupiera). Revalidado con el mismo tipo de test sintético de marcadores de color (facecam_region y esquina opuesta reales de config, factor de zoom exagerado a 1.4 para medir con margen, dos tramos: uno de 8s y otro de 2s < ramp): la rampa reproduce la curva coseno esperada en los puntos intermedios (25%/50%/75% -> zoom lineal 1.057/1.200/1.335, consistente con `0.5*(1-cos(...))`), alcanza el pico exacto (7.245% ~ 3.696%*1.4²) exactamente en t=start+ramp_seconds, y en el frame siguiente (1/30s después) cae de golpe a baseline — permaneciendo en baseline el resto del tramo (sin sostenido) y también en el tramo de 2s (sin zoom en absoluto, ninguno de sus frames se despega del baseline). **Rediseño en dos pasos (2026-08-05):** el diseño de "todo en un solo `filter_complex`" (corte + zoom en una sola pasada de ffmpeg) falló en producción contra `dinoblade_1` (grabación real de 1h39m, 181 cortes + 47 tramos de zoom): `FileNotFoundError [WinError 206]` ("el nombre del archivo o la extensión es demasiado largo") porque el filtro generado (44051 caracteres) supera el límite de línea de comandos de Windows (~32767 caracteres vía `CreateProcess`); confirmado además que este build de ffmpeg no soporta `-filter_complex_script` ni el indirect `@archivo` para `-filter_complex` (ambos devuelven "option not found"), así que no hay forma de sacar el grafo de filtros de la línea de comandos. Antes de rediseñar se probó empíricamente el enfoque "obvio" para evitar `filter_complex` del todo — el concat demuxer de ffmpeg con `inpoint`/`outpoint` directamente sobre el vídeo original — con un vídeo sintético de marcadores por frame y GOP forzado (keyframes cada 0.8s, puntos de corte deliberadamente a mitad de GOP: se descartó porque el `inpoint`, al no alinear con un keyframe, NO recorta con precisión de frame al recodificar: salta al keyframe anterior e incluye de más TODOS los frames intermedios (hasta un GOP entero de más, ~0.8s en la prueba) — exactamente lo que CLAUDE.md prohíbe (comerse habla/acción por un corte impreciso). El `outpoint` sí es exacto; el problema es específico del `inpoint`, y no existe ninguna opción del demuxer concat (`-h demuxer=concat`: solo `safe`/`auto_convert`/`segment_time_metadata`) para forzarlo a ser preciso. Diseño final adoptado en su lugar: (1) **corte** — se mantiene el mecanismo YA VALIDADO (`trim`/`atrim`+`setpts`+`concat`), pero PARTICIONADO en "shards": cada shard cubre un subconjunto consecutivo de tramos tal que su `filter_complex` no supere `_MAX_FILTER_COMPLEX_CHARS` (20000, con margen bajo el límite de Windows), y además acota su propia decodificación con `-ss <inicio_shard> -to <fin_shard> -copyts` antes de `-i` (misma lección de rendimiento que `detect_cuts`: sin esto, cada shard re-decodificaría el vídeo completo, multiplicando el coste por el nº de shards); los shards se pegan después con el concat demuxer SOBRE ARCHIVOS YA COMPLETOS (sin `inpoint`/`outpoint`, solo `file '...'` + `-c copy`) — rápido y exacto porque no recorta dentro de ningún archivo, solo concatena archivos ya cortados (mismo mecanismo, ya usado y validado, de la vía rápida de `append_outro`). (2) **zoom** — se aplica en un `filter_complex` APARTE sobre el vídeo ya cortado, mucho más corto porque solo cubre los tramos de habla larga (decenas, no cientos); el audio no pasa por este filtro (se copia con `-c:a copy`, sin recodificar). Si algún día hicieran falta más tramos de zoom de los que caben en un único `filter_complex`, se particiona igual y se encadenan varias pasadas (cada una sobre la salida de la anterior). Validación exhaustiva antes de dar por bueno el rediseño: (a) mecanismo de corte por shards verificado con un clip sintético de marcadores de frame exacto (10s, GOP forzado no alineado con los puntos de corte) comparando nº de frames EXACTO (ffprobe `-count_frames`) y contenido (primer/último frame decodificado) entre la versión sin particionar y la particionada en 3 shards desiguales — resultado IDÉNTICO; (b) el código de producción real (`_cut_video`, forzado a 7 shards de 1 tramo cada uno vía `__defaults__`) reproduce EXACTAMENTE (mismo nº de frames y contenido) el resultado sin particionar; (c) `_apply_zoom` (código de producción, forzado a 7 pasadas encadenadas de 1 tramo cada una) produce un vídeo PIXEL-IDÉNTICO (hash SHA256 del contenido decodificado igual) al de una única pasada sin particionar; (d) test de escala sintético (400 cortes + 50 tramos de zoom sobre 1h de vídeo sintético, más del doble de cortes que el caso real que falló): completado en 181.8s sin ningún fallo de línea de comandos, repartido automáticamente en 4 shards de corte + 1 pasada de zoom, duración de salida correcta a 0.02s; (e) **ejecución completa real contra `dinoblade_1`** (1h39m/181 cortes/47 tramos de zoom, el caso que originalmente falló): pipeline completo (corte en 2 shards + zoom en 1 pasada + loudnorm; sin outro, archivo no presente) terminado con éxito en ~25m20s, `final.mp4` de 5532.90s (esperados 5532.79s, diff 0.11s) a 1920x1080@60fps/AAC 48kHz estéreo, todos los intermedios limpiados correctamente (solo queda `final.mp4`), verificado además que decodifica sin errores en varios puntos del timeline (incluido justo al inicio de un tramo de zoom). Sin límite práctico de nº de cortes/tramos: al superar el presupuesto de caracteres simplemente se añaden más shards/pasadas en vez de fallar; a escala real (181-400 cortes) hacen falta solo 2-4 shards. Contrapartida aceptada conscientemente: dividir en dos pasadas de ffmpeg añade una generación extra de recodificación de vídeo (antes: 1 recodificación; ahora: corte a crf16/veryfast (calidad alta, para no acumular pérdida con la recodificación del zoom) + zoom a crf20/medium (mismo target de calidad final que antes) — más trabajo total de ffmpeg que el diseño original de una sola pasada, pero es la consecuencia directa e inevitable de partir en dos pasos independientes. **Discontinuidad de PTS en shards grandes — primera ronda (2026-08-08):** una ejecución real posterior contra `dinoblade_1` (con este mismo diseño de shards, particionados SOLO por presupuesto de caracteres) produjo un `final.mp4` que se congelaba en el reproductor sobre el minuto 10:29 y saltaba al minuto ~67, sin audio a partir de ahí; `ffprobe` reveló un salto de PTS de ~3400s en mitad del vídeo, aterrizando casi exactamente en la duración total del shard que lo contenía (133 tramos concatenados de un tirón en un único `filter_complex`). Reproducido con un test sintético de escala similar (1h/400 cortes/60fps, `tests/scale_test_edit_pipeline.py`) pero NO con <=37 tramos de footage real de `dinoblade_1` (1080p60) ni con 133 tramos sintéticos a 30fps/640x360. Primer fix aplicado: además del presupuesto de caracteres, cada shard de corte se limitó a `_MAX_CONCAT_SEGMENTS_PER_SHARD` (40) tramos como máximo — validado entonces solo contra el test sintético. **Segunda ronda — el primer fix no bastaba (2026-08-08):** al relanzar la generación real COMPLETA de `dinoblade_1` con ese fix aplicado y verificar con `ffprobe` TODO el archivo resultante (no solo el punto que había fallado antes), seguían apareciendo 2 discontinuidades de PTS de vídeo (saltos de 25.6s y 519.5s) — el límite de 40 tramos/shard NO bastaba a la resolución real del proyecto (1920x1080), aunque sí bastaba en el test sintético a 640x360. Aislando el shard problemático (los mismos 40 tramos, mismo código de producción `_cut_shard`, sin el resto del pipeline alrededor) se encontró la causa real: el archivo de ESE SHARD, por sí solo, tenía 1077.5s de vídeo pero 1587.8s de audio — el filtro `concat` de ffmpeg estaba perdiendo ~510s de FRAMES DE VÍDEO silenciosamente dentro de su propio `filter_complex` (la pista de audio, con el mismo fan-in, no se veía afectada en absoluto). Es decir, no es (solo) que el `concat` pierda la cuenta del PTS acumulado como se pensó en la primera ronda, sino un fallo del propio filtro al reunir muchas ramas de vídeo `trim+setpts`, sensible aparentemente también a la resolución (1080p reproduce el fallo con 40 tramos, algo que 640x360 no reproducía ni con 133) — por lo que NINGÚN presupuesto de nº de tramos por shard es una cota fiable mientras se siga usando el filtro `concat` para el corte. **Fix definitivo: eliminar el filtro `concat` del corte por completo, no acotar su fan-in — evitarlo.** `_cut_video` ya no arma ningún `filter_complex` para cortar; cada tramo se recorta a su propio archivo aislado con un simple trim de entrada (`_cut_segment`: `-ss`/`-to` antes de `-i`, preciso a nivel de frame al recodificar, sin ningún filtro) y los archivos resultantes se pegan con el concat DEMUXER (`_glue_video_files`, mismo mecanismo ya usado y validado en la vía rápida de `append_outro` y para pegar shards entre sí), que opera a nivel de contenedor (paquetes ya completos) y nunca pasa por el filtro `concat`. Sin límite práctico de nº de tramos: cada uno es una llamada de ffmpeg independiente y corta, así que ni el nº de tramos ni sus caracteres pueden acercar la línea de comandos al límite de Windows (`_MAX_FILTER_COMPLEX_CHARS`/`_plan_zoom_passes` siguen aplicando solo al paso de zoom, que nunca usó `concat` y no mostró el problema). **Revalidación final:** revalidado primero con el test de escala sintético (400 cortes/60fps, 640x360) tras el rediseño — OK, sin discontinuidades (evidencia débil por sí sola dada la sensibilidad a resolución del bug original, pero confirma que el mecanismo nuevo funciona estructuralmente); y después con una ejecución completa real de `dinoblade_1` (182 tramos — un archivo aislado por tramo — + zoom en 1 pasada + loudnorm; sin outro, archivo no presente), completada en ~56 min, `final.mp4` de 7.36GB / 5535.71s de duración a 1920x1080@60fps/AAC 48kHz estéreo (vs. ~5532.78s ideal por suma de tramos conservados — la diferencia de ~2.9s es redondeo esperado a límite de frame por tramo con `-ss`/`-to`, sistemáticamente hacia MÁS contenido conservado nunca menos, así que la dirección sigue siendo segura). Verificado con `ffprobe` PAQUETE A PAQUETE sobre TODO el archivo (vídeo y audio, no solo el punto que había fallado antes, reutilizando `check_pts_continuity`/`check_av_duration_consistency` de `tests/scale_test_edit_pipeline.py`): DTS monótono no decreciente y ningún hueco de PTS ordenado > 1s en ningún punto del archivo, y duración de vídeo/audio consistente entre sí (diferencia < 2s) — **0 discontinuidades de PTS en todo el archivo**. **Renderizado parcial sin pérdida en el corte (smart cut, 2026-08-09, idea tomada de auto-editor de WyattBlue):** investigado primero con un prototipo aislado (sin tocar este módulo) antes de implementarlo, a petición del usuario, para no arriesgar el pipeline en producción sin evidencia. `_cut_segment` (recodificaba SIEMPRE el tramo completo a conservar, crf16/veryfast) se sustituyó por `_cut_segment_smart`: busca el primer keyframe real >= inicio del tramo y el último <= fin del tramo (`_scan_keyframe_timestamps`, un único `ffprobe` por vídeo que solo demuxea paquetes sin decodificar — barato incluso en grabaciones de 1-2h); si hay hueco entre ambos, copia ese interior SIN RECODIFICAR (`-c copy`, prácticamente gratis) y solo recodifica los bordes hasta esos keyframes (o el tramo completo, como antes, si es más corto que un intervalo de keyframe y no hay hueco útil — mismo fallback de siempre, sin pérdida de robustez). Todos los fragmentos resultantes (1 a 3 por tramo) se pegan con el mismo concat DEMUXER (`_glue_video_files`) ya validado, sin ningún mecanismo nuevo de unión. **Impacto medido antes de implementar** (investigación con los `cuts.json` reales y los keyframes reales del vídeo de entrada, sin decodificar — `ffprobe` completo: ~15s para `dinoblade_1` de 5913s, ~33s para `icarus_1` de 7729s): 91.2% de la duración conservada de `dinoblade_1` (147 cortes) es candidata a copia directa, 76.0% en `icarus_1` (549 cortes, mucha más densidad de cortes — la fracción no copiable se concentra en tramos más cortos que un intervalo de keyframe, no en el grueso de la duración). **Validado con un test sintético dedicado** (`tests/test_smart_cut_segments.py`, mismo patrón que `scale_test_edit_pipeline.py`: genera su propia fuente con GOP forzado y color range completo (`yuvj420p`, igual que los `raw.mp4` reales) sin tocar `data/`): confirma que el interior copiado es BIT-IDÉNTICO al origen (hash del frame decodificado), que `pix_fmt`/`color_range` se conservan consistentes entre fragmentos recodificados y copiados (sin salto de niveles en la costura — comprobado explícitamente que ffmpeg propaga el rango completo automáticamente incluso sin flags `-color_range` adicionales), que el resultado de `_cut_video` real sigue pasando `check_pts_continuity`/`check_av_duration_consistency` (reutilizadas de `scale_test_edit_pipeline.py`) sin discontinuidades, y que el recuento de frames coincide con un baseline que recodifica todo (con la tolerancia esperada del mismo tipo de redondeo ya aceptado en producción). **Revalidado además contra `dinoblade_1` real completo** (script de validación aparte, sin tocar `data/output/`): comparando el `_cut_video` real de producción (smart) contra un baseline que recodifica los 147 tramos completos con el mismo código (`_cut_segment_recode`, sin la división cabeza/interior/cola) — **4.01x más rápido** (252.7s / 4.2 min smart vs. 1012.9s / 16.9 min baseline, ahorro de ~12.7 min SOLO en el paso de corte), `check_pts_continuity`/`check_av_duration_consistency` sin problemas sobre el resultado real, y +176 frames (2.93s a 60fps) de diferencia en el recuento total frente al baseline — mismo tipo de redondeo por tramo dividido ya aceptado (a favor de conservar más, nunca menos), aquí más visible porque 68/147 tramos tienen hueco interior copiable y por tanto un punto de corte extra cada uno. Con el zoom desactivado por defecto (`long_speech_zoom_factor: 1.0`, ver más abajo), este paso de corte es hoy el cuello de botella dominante del pipeline, así que la mejora aplica casi directamente al tiempo total de `edit.run`. **Bug de discontinuidad de PTS en `append_outro` — encontrado y corregido (2026-08-09):** durante la primera ejecución real completa e integrada de todo el pipeline (detect_cuts -> edit -> detect_chapters -> subtitles -> thumbnail) contra `dinoblade_1`, la verificación final con `check_pts_continuity` (reutilizada de `scale_test_edit_pipeline.py`) encontró un salto de PTS de vídeo de **3448s** en mitad de `final.mp4` (entre 1155.85s y 4603.92s; audio intacto, solo vídeo) — el mismo síntoma exacto ya documentado arriba para el paso de corte ("el filtro `concat` de ffmpeg pierde frames de vídeo en 1080p"). Causa: `assets/outro/outro.mp4` (1080p30/44.1kHz) no coincide en formato con el clip principal (1080p60/48kHz) recién añadido a `assets/outro/` — nunca antes había un outro real presente en una ejecución completa, así que el camino lento de `append_outro` (activo cuando el outro no coincide en parámetros) nunca se había ejercido contra un vídeo real; ese camino pasaba el clip principal COMPLETO (~1h16m) junto con el outro por un único filtro `concat` de ffmpeg para poder recodificar el outro y unirlos en una sola pasada, reproduciendo el mismo bug de pérdida de frames ya resuelto para el corte pese a ser solo 2 ramas (no las decenas de `trim`+`setpts` del bug original) — aparentemente la combinación resolución 1080p + rama larga basta para dispararlo, no hace falta un fan-in grande. Fix (mismo principio que el fix del corte: evitar el filtro `concat`, no acotarlo): el camino lento ahora recodifica ÚNICAMENTE el outro (archivo corto) a un fichero aparte con los parámetros del clip principal, y une ambos con el concat DEMUXER ya validado (mismo mecanismo que la vía rápida de esta función y que `_glue_video_files`) — el clip principal nunca se toca ni se recodifica. Validado con un test de regresión dedicado (`tests/test_append_outro_pts.py`, clips sintéticos con resolución/fps/sample_rate/canales deliberadamente distintos entre sí): duración del resultado = suma de ambos clips, resolución/fps del resultado = los del clip principal, sin discontinuidades de PTS (`check_pts_continuity`/`check_av_duration_consistency`), y el elementary stream de vídeo del resultado EMPIEZA con los mismos bytes exactos que el clip principal original (extraído con `-bsf:v h264_mp4toannexb`, comparación byte a byte — la prueba más directa de que `-c copy` no tocó el clip principal). Nota de proceso: la primera versión de este test comparaba hashes de FRAMES DECODIFICADOS en vez de bytes crudos y dio un falso negativo (la decodificación con reordenamiento de B-frames no resultó ser determinista frame-a-frame entre streams de duración distinta, pese a que el elementary stream subyacente sí era idéntico) — corregido comparando el ES crudo directamente. Revalidado contra `dinoblade_1` real completo tras el fix: reejecutado `edit.run` (~23 min) y verificado con `ffprobe` paquete a paquete sobre TODO el archivo resultante — **0 discontinuidades de PTS**, `final.mp4` de 4613.94s / 8.26GB (vs. 1.58GB de la versión rota, que había recodificado casi todo el clip a crf20 en vez de conservar la calidad crf16/copy del corte) |
| subtitles          | ✅ | ✅ | Implementado (2026-08-09): `filter_and_remap_words` descarta las palabras de `transcript['words']` cuyo intervalo ORIGINAL solapa (total o parcialmente) con algún corte de `cuts.json`, y remapea las que sobreviven a la línea de tiempo editada con `map_to_edited_timeline` (mismo mecanismo que `edit/`/`detect_chapters/`). `_group_words_into_cues` las agrupa en subtítulos respetando un presupuesto de caracteres (`max_chars_per_line * max_lines`, 84 por defecto) y de duración (`max_cue_seconds`, 6s); cuando hace falta cortar por desbordar cualquiera de los dos, corta en la ÚLTIMA pausa natural vista dentro del grupo (puntuación de frase/cláusula, o hueco >= `natural_pause_gap_seconds`) en vez de justo en la palabra que desborda, salvo que no haya ninguna pausa útil (corte duro como último recurso). `build_cues` extiende subtítulos demasiado cortos hasta un suelo de duración derivado del ritmo de lectura de referencia (`reading_cps_max`, 20 cps — SOLO el límite superior de la banda 15-20 se aplica como suelo; el inferior no se usa como techo porque alargar de más dejaría el subtítulo en pantalla después de que la persona ya haya dicho otra cosa), sin solapar nunca con el siguiente subtítulo ni recortar por debajo de la duración real de la locución. `wrap_cue_text` reparte el texto en <= 2 líneas de <= 42 caracteres, buscando el punto de corte más equilibrado entre las dos líneas. Genera `data/output/<video_id>/subtitles.srt` (formato .srt estándar, numeración secuencial, `HH:MM:SS,mmm`). `config['subtitles']['enabled']` (default true) desactiva el módulo sin tocar código. Validado con `tests/test_subtitles.py` (descarte/remapeo de palabras cortadas incluido un caso de solape parcial en el borde, preferencia de pausa natural por caracteres y por hueco largo, suelo de duración por lectura y tope por siguiente subtítulo/duración total, envoltura balanceada en 2 líneas, formato `.srt`, y una ejecución `run()` end-to-end contra transcript/cuts sintéticos verificando que ningún subtítulo excede los límites de líneas/caracteres/duración y que la palabra cortada no aparece) y con una ejecución real completa contra `dinoblade_1`: 632 subtítulos a partir de 5557/6188 palabras conservadas (<1s de proceso). **Bug de sincronización encontrado en revisión manual del vídeo real (2026-08-09): los subtítulos aparecían sistemáticamente ADELANTADOS respecto al audio.** Investigado retranscribiendo con faster-whisper (modelo `small`, sin tocar `data/`) tres ventanas reales de `final.mp4` (a ~26min/48min/73min) y comparando la posición REAL de las palabras contra el timestamp que `subtitles.srt` les había asignado: desfase de +1.95s/+3.65s/+4.29s (subtítulo mostrado ANTES de que se dijera la palabra), creciendo con la posición en el vídeo. Causa raíz: `map_to_edited_timeline` asume que cada corte de `cuts.json` elimina EXACTAMENTE `end-start` segundos, pero el "renderizado parcial sin pérdida" de `edit/run.py` no corta así de exacto -- su recodificación de cabeza/cola con `-ss`/`-to` redondea al frame más cercano, error pequeño por corte (menos de un frame) pero SIEMPRE en la misma dirección ("más contenido conservado", ya documentado en la fila `edit` de más abajo) y ACUMULATIVO: con pocos cortes (test sintético) es inapreciable, con los 146 cortes reales de `dinoblade_1` crece hasta ~5.3s al final del vídeo. Comparando el modelo de deriva "proporcional al tiempo transcurrido" contra "proporcional al Nº DE CORTES ya pasados" en los mismos 3 puntos de control, el segundo ajustaba claramente mejor (consistente con el mecanismo real: la deriva se genera EN cada corte, no con el paso del tiempo). Fix: `compute_drift_per_cut` mide la ÚNICA cosa comprobable sin volver a cortar nada -- la diferencia entre la duración nominal de `cuts.json` y la duración REAL del contenido principal en el `final.mp4` ya generado (restando la duración del outro si aplica) -- y reparte esa diferencia a partes iguales entre los cortes; `_calibrated_edited_timestamp` suma esa corrección lineal según cuántos cortes ha pasado ya cada palabra. Es una aproximación (no frame-perfecta: el redondeo real de cada corte varía un poco), con guarda de sanidad (`_MAX_PLAUSIBLE_DRIFT_FRACTION`) que desactiva la calibración si la deriva medida es implausible, y cae a 0.0 (comportamiento idéntico al de antes del fix) si `final.mp4` no existe todavía -- nunca bloquea ni rompe la generación de subtítulos. Verificado: (a) `tests/test_subtitles.py` ampliado con `compute_drift_per_cut`/`_calibrated_edited_timestamp` contra un `final.mp4` sintético REAL de duración conocida (5 cortes, deriva total 0.5s) confirmando el reparto lineal exacto y los 3 casos de fallback (sin final.mp4, deriva implausible, drift_per_cut=0.0 idéntico al remapeo sin calibrar), más una ejecución `run()` end-to-end verificando que los 6 cues quedan en los timestamps calibrados esperados y que el desplazamiento CRECE con el nº de cortes pasados (no es un shift uniforme); (b) regenerado `subtitles.srt` real de `dinoblade_1` con el fix y revalidados los 3 mismos puntos de control contra las mismas retranscripciones de referencia: desfase bajó de +1.95/+3.65/+4.29s a +0.14/-0.32/-0.61s -- mejora de un orden de magnitud, aunque no frame-perfecta (limitación esperada de una calibración lineal, documentada en el módulo) |
| thumbnail          | ✅ | ✅ | **Simplificado drásticamente (2026-08-09), tercera versión de este módulo -- sustituye por completo las dos anteriores** (primero paneles con borde + titular vía Claude + mejora con Gemini; después recorte de sujeto vía `rembg` + fondo desenfocado + título quemado -- ver el historial de git de este archivo/de `src/thumbnail/run.py` si hace falta el detalle de esos diseños, ya no vigentes en el código). A petición explícita del usuario tras revisar el resultado del diseño anterior: el módulo YA NO COMPONE NADA -- se limita a EXTRAER frames candidatos reales de momentos de alto movimiento, sin recortar, sin excluir ninguna zona del frame, sin redimensionar, y el usuario compone la miniatura final a mano con su propio editor. `_select_candidate_frames` muestrea `config['thumbnail']['candidate_sample_count']` (default 60) puntos por el vídeo (evitando el primer/último 10% y cualquier tramo ya en `cuts.json`, igual que las versiones anteriores) y puntúa cada uno por MOVIMIENTO -- diferencia media de píxeles en gris respecto al frame ~0.5s antes, la misma señal ligera ya usada en la versión anterior para el frame de gameplay, reutilizada tal cual -- pero ahora sobre el FRAME COMPLETO, sin excluir `facecam_region` de nada (streamer y juego deben poder aparecer juntos, tal cual se grabó). De mayor a menor puntuación, se van aceptando candidatos mientras no queden a menos de `--min-gap-seconds` (default 60s) de uno ya elegido (criterio greedy), hasta `--num-candidates` (default 5) o hasta agotar los muestreados. `run()` guarda cada uno con `cv2.imwrite` (PNG, sin pasar por Pillow -- ya no hace falta, no hay composición) como `data/output/<video_id>/thumbnail_candidate_<i>.png` (1-indexado, orden CRONOLÓGICO aunque la selección internamente sea por puntuación), limpiando primero cualquier candidato de una ejecución anterior con más candidatos que la actual. **IMPORTANTE -- este módulo NUNCA crea ni toca `data/output/<video_id>/thumbnail.png`:** esa ruta la sigue leyendo `publish/youtube.py` sin cambios, pero ahora es SIEMPRE obligatoria -- `_thumbnail_path` lanza `FileNotFoundError` con un mensaje claro ("thumbnail.png no encontrado, generar/elegir uno primero", con el comando exacto a ejecutar) si no existe todavía, en vez de subir sin miniatura en silencio como hacía antes (ver fila `publish`). **Eliminado en esta simplificación:** segmentación con `rembg` (recorte de sujeto), descarga/cacheado de fuentes (Nosifer y demás), fondo desenfocado/oscurecido, título quemado, y cualquier resto de versiones anteriores (Gemini/Nano Banana, titular por transcripción con Claude, logo del juego) -- `_cut_out_subject`/`_paste_subject`/`_draw_title`/`_prepare_background`/`_remove_facecam_overlay` ya no existen. `requirements.txt`: fuera `rembg` y `pillow` (confirmado por grep que ya no los usa ningún otro módulo del proyecto). `config['thumbnail']['face_candidate_segments']`/`['gameplay_candidate_count']` retirados de `settings.yaml` (ya no hay selección de frame de cara ni de gameplay por separado, ni vistosidad) -- sustituidos por `candidate_sample_count`. CLI simplificado a `--video-id`/`--num-candidates`/`--min-gap-seconds`. Validado con `tests/test_thumbnail.py` reescrito (18 comprobaciones): un vídeo sintético con 4 ráfagas de movimiento dispersas -- incluida una DENTRO de lo que en las versiones anteriores habría sido `facecam_region`, confirmando que ya no se excluye ninguna zona -- confirma que se detectan las ráfagas correctas, que dos ráfagas más cercanas entre sí que `min_gap_seconds` colapsan en un solo candidato (el de mayor movimiento de las dos, no un empate arbitrario), y que una ráfaga dentro de un tramo de `cuts.json` se descarta; `run()` confirma nombres de archivo (`thumbnail_candidate_1.png`... en orden cronológico), resolución EXACTA del vídeo de origen (sin redimensionar), limpieza de candidatos sobrantes de una ejecución anterior con más candidatos, que nunca toca `thumbnail.png`, y que `enabled=False` no genera nada. Revalidado con una extracción real completa contra `dinoblade_1` (`--num-candidates 5 --min-gap-seconds 60`, valores por defecto): terminada en ~13s, 5 candidatos a 1920x1080 (resolución completa del vídeo original) en momentos de combate claramente distintos y bien repartidos por la segunda mitad del vídeo (t≈2756s/3798s/4841s/5001s/5162s) -- enseñados al usuario para su revisión antes de dar el módulo por bueno. Nota de alcance (documentada en el módulo, no un defecto): la selección es puramente por movimiento, sin ningún criterio de "vistosidad"/calidad visual de las versiones anteriores -- de los 5 candidatos reales, 4 muestran acción claramente dramática y 1 es un momento de acción más ambiguo visualmente (una caída/salto contra fondo neblinoso); el usuario elige a mano cuál usar, así que no se consideró necesario reintroducir ese criterio |
| publish            | ✅ | ⏳ | Implementado (2026-08-09): `src/publish/youtube.py` sube `data/output/<video_id>/final.mp4` vía YouTube Data API v3 (`videos().insert`, `MediaFileUpload(chunksize=-1, resumable=True)`, bucle de subida con `_execute_resumable_upload` que reintenta con backoff exponencial ante 5xx transitorios y propaga cualquier otro error sin reintentar). `_build_video_body` (función pura) arma snippet/status: título por parámetro o un placeholder `[TÍTULO PENDIENTE] <video_id>` si no se da ninguno (nunca un título inventado), descripción = contenido de `chapters.txt` si existe (vacía si no), `privacyStatus` SIEMPRE `"private"` salvo que se pida explícitamente `"unlisted"`/`"public"`. Si `thumbnail.png` existe, se adjunta con `thumbnails().set()` tras la subida (necesita el id que devuelve YouTube). OAuth vía `google-auth-oauthlib` (`YOUTUBE_CLIENT_SECRET_PATH` del `.env`, scope `youtube.upload`, token cacheado en `token.json`/gitignored, refresco automático). **Salvaguarda deliberada — no es solo "no lo ejecuté esta vez":** `run()` tiene `execute=False` como valor por defecto real del parámetro (no un flag opcional que haya que recordar activar); con `execute=False` se construye la petición completa (credenciales, servicio de la API vía `googleapiclient.discovery.build` — confirmado que funciona sin red gracias al discovery document estático que empaqueta `google-api-python-client` para APIs conocidas como youtube v3 — body, `MediaFileUpload`) y se detiene ahí sin llamar a `.execute()`/`next_chunk()` en ningún punto; solo `execute=True` (`--execute` en el CLI) ejecuta la subida real. `youtube_service` es inyectable (mismo patrón `client` que `thumbnail`/`detect_chapters`) para testear la construcción de la petición sin OAuth ni red. Validado con `tests/test_publish_youtube.py` (21 comprobaciones, servicio de YouTube falso inyectado): `_build_video_body` puro (placeholder/título dado/descripción/privacidad), helpers de filesystem (`_final_video_path` exige `final.mp4`, `_description_from_chapters`/`_thumbnail_path` opcionales), `run(execute=False)` construye exactamente 1 petición `videos().insert` con el body correcto y JAMÁS llama a `next_chunk()` ni a `thumbnails().set()`, `run(execute=True)` agota `next_chunk()` hasta la respuesta final y adjunta la miniatura con el id devuelto por YouTube (y NO la adjunta si no hay `thumbnail.png`), y `_execute_resumable_upload` reintenta ante `HttpError 503` pero propaga sin reintentar un `HttpError 404`. **No probado contra la API real de YouTube ni contra OAuth real:** por instrucción explícita del usuario, no se ha ejecutado ninguna subida real (ni siquiera de prueba) — la subida real la confirmará el usuario explícitamente más adelante. **Subtítulos añadidos (2026-08-09):** si `data/output/<video_id>/subtitles.srt` existe, se sube como pista de captions vía `captions().insert()` (idioma `es` por defecto, parametrizable con `caption_language`/`--caption-language`, `isDraft=False`) DESPUÉS del vídeo y de la miniatura, reutilizando el `youtube_video_id` devuelto por la subida. A diferencia del resto de la petición (que SIEMPRE se construye aunque `execute=False`), `captions().insert()` no se construye ni se llama en absoluto con `execute=False` — no existe ningún `youtube_video_id` todavía sobre el que construirla. Test ampliado con un servicio de captions falso: confirma que con `execute=False` nunca se llama (aunque exista `subtitles.srt`), que con `execute=True` se llama una vez con el `videoId`/idioma/`isDraft` correctos, que el idioma pasado por parámetro se propaga, y que sin `subtitles.srt` no se llama en absoluto (`subtitles_attached=False`) **thumbnail.png pasa a ser SIEMPRE obligatoria (2026-08-09), ya no opcional:** desde que `src/thumbnail/run.py` dejó de generar la miniatura final automáticamente (ver fila `thumbnail`), subir un vídeo sin haber elegido/creado antes `thumbnail.png` a mano sería fácil de hacer sin darse cuenta; `_thumbnail_path` ahora lanza `FileNotFoundError` (mensaje: "thumbnail.png no encontrado, generar/elegir uno primero", con el comando exacto de `src/thumbnail/run.py` a ejecutar) si no existe todavía, ANTES de construir ninguna petición -- tanto con `execute=False` como con `execute=True` -- en vez de subir sin miniatura en silencio como hacía antes. `subtitles.srt` SIGUE siendo opcional (no bloquea la subida si falta). Tests actualizados: `_thumbnail_path` sin `thumbnail.png` confirma la excepción y su mensaje; `run()` confirma que falla igual en ambos modos de `execute` y que nunca llega a construir ninguna petición `videos().insert()`; un caso nuevo con `thumbnail.png` pero SIN `subtitles.srt` confirma que la subida sigue funcionando con normalidad (adjunta la miniatura, omite captions). |
| intro (grabado aparte) | ✅ | ✅ | Implementado (2026-08-10, simétrico al outro): `data/output/<video_id>/intro.mp4` opcional -- si no existe, el pipeline se comporta exactamente igual que antes de esta funcionalidad (verificado por los tests existentes sin modificar, que siguen pasando sin cambios). **Generalización del mecanismo de unión (`src/edit/run.py`):** `_same_stream_params`/el `append_outro` binario original (fast-identical / full-recode) se sustituyen por `_glue_extra_clip` (posición `"before"`/`"after"`), TRES niveles -- mismos parámetros de vídeo Y audio (concat demuxer, sin recodificar nada), solo el AUDIO difiere (recodifica únicamente el audio del clip extra), o el vídeo difiere (recodifica el clip extra COMPLETO, escalado SIEMPRE a la resolución del clip PRINCIPAL, nunca al revés) -- generalizado a partir de `_smart_concat`, validado antes a mano en `data/work/shift_at_midnight_1/run_pipeline.py` contra un caso real con audio distinto. `prepend_intro` (nuevo) antepone el intro con este mecanismo; `append_outro` se reescribió como envoltorio fino sobre el mismo `_glue_extra_clip` (posición `"after"`) -- revalidado que `tests/test_append_outro_pts.py` (ya existente, sin tocar) sigue pasando igual tras el refactor. `run()` encadena `apply_cuts_with_zoom -> normalize_audio -> prepend_intro -> append_outro`. Nuevo `config['edit']['prepend_intro']` (default true). `detect_chapters`: `remap_chapters_to_edited_timeline` gana `intro_duration_s` (default 0.0, retrocompatible) -- con intro real antepone SIEMPRE "Introducción" fija en 0:00 y desplaza el resto +intro_duration_s (leído con ffprobe directo de `intro.mp4`, nominal, porque este módulo puede correr antes que `edit/`), reaplicando la separación mínima. `subtitles`: núcleo de faster-whisper extraído de `src/transcribe/run.py` a `src/common/transcription.py` (`transcribe_file`, compartido sin que un módulo importe lógica de negocio de otro directamente, ver CLAUDE.md) para poder transcribir el intro bajo demanda sin persistir a disco; sus palabras se anteponen al `.srt` sin remapear (el intro no pasa por `cuts.json`), y las del contenido principal se desplazan +intro_duration_s DESPUÉS de la calibración de deriva ya existente -- `compute_drift_per_cut` resta `intro_duration_s` de `final.mp4` exactamente igual que ya restaba la duración del outro, así que cualquier imprecisión de `intro_duration_s` se absorbe en la misma corrección lineal en vez de quedar como desplazamiento fijo sin corregir. `thumbnail`: sin cambios de código -- los candidatos ya salían exclusivamente de `data/raw/<video_id>.mp4`, que nunca incluye el intro, así que quedan excluidos por construcción (documentado explícitamente en el docstring del módulo). **Validado:** unitarios nuevos/ampliados sin red ni vídeo salvo síntesis con ffmpeg -- `tests/test_prepend_intro.py` (nuevo, mismo patrón que `test_append_outro_pts.py`: los tres niveles de `_glue_extra_clip` vía `prepend_intro`, retrocompatibilidad sin `intro.mp4`, toggle de config, clip principal siempre bit-idéntico -- comparando el FINAL del elementary stream, ya que el principal va segundo en la lista del concat cuando `position="before"`), `tests/test_detect_chapters.py` (`intro_duration_s=0.0` reproduce el resultado exacto sin el parámetro; con intro real la "Introducción" fija nunca colapsa con un capítulo detectado cerca de 0 salvo que la separación tras desplazar siga sin llegar a `min_chapter_seconds`), `tests/test_subtitles.py` (`_prepare_intro_words` descarta palabras vacías/de duración nula; `run()` end-to-end con `transcribe_file` monkeypatcheado -- sin modelo real -- confirma 2 cues del intro sin desplazar + 6 del contenido principal desplazados +intro_duration_s, en orden cronológico). **Verificación end-to-end real** (`data/work/introtest_1` en el work_dir de un video_id aislado, no toca `dinoblade_1`/`icarus_1`/`shift_at_midnight_1` reales): clip principal = 90s reales de habla en español extraídos de `data/raw/dinoblade_1.mp4` (t=1150-1240, 1920x1080@60fps sin tocar), intro = 15s reales de la MISMA fuente (t=1310-1325) pero recodificados deliberadamente a 1280x720@25fps/44.1kHz mono para forzar el nivel `full_recode` -- exactamente el escenario que describe el usuario (intro grabado en otra sesión de OBS). Pipeline completo real (`ingest` -> `transcribe` modelo `tiny` -> `detect_cuts` -> `edit` con intro+outro real de `assets/outro/outro.mp4`, TAMBIÉN con resolución/fps distintos (1080p30) -> `detect_chapters` con la API real de Claude -> `subtitles` -> `thumbnail`) terminado sin errores: `final.mp4` a 1920x1080@60fps (resolución del clip principal, ni el intro ni el outro la alteran) de 108.64s (83.42s de contenido principal tras 3 cortes + 15.00s de intro + 10.01s de outro, ambos glue paths -- intro Y outro -- ejercitados de verdad en el mismo run); `chapters.txt`/`chapters.json` con "00:00 Introducción" (el único capítulo que Claude propuso para un clip de 90s quedó a menos de `min_chapter_seconds` de la intro tras desplazarse y se descartó correctamente por la misma regla de separación mínima -- comportamiento esperado, no un fallo, dada la duración corta del clip de prueba); `subtitles.srt` con los 3 primeros cues siendo la transcripción real (modelo `tiny`, calidad menor que `medium` pero reconocible) del intro desde 0:00, y el resto del contenido principal arrancando correctamente desplazado tras el intro; 3 candidatos de miniatura (t=33s/52s/79s) dentro de los 90.03s de `data/raw/introtest_1.mp4` (el intro nunca se abre para esto). **Bug encontrado y corregido durante esta verificación real (no detectado por los tests unitarios porque el módulo `logging` de Python captura errores de formato internamente en vez de propagarlos):** el log de `subtitles/run.py` al detectar el intro tenía 3 placeholders (`%s`/`%.2f`/`%.2f`) pero solo 2 argumentos -- corregido pasando `intro_duration_s` dos veces (ruta, duración, duración desplazada). Resultados completos mostrados al usuario (logs + contenido de `chapters.txt`/`chapters.json`/primeros cues de `subtitles.srt`/rutas de candidatos) antes de dar el trabajo por bueno; sin commit todavía a petición explícita del usuario. |

## Decisiones de diseño registradas

- El vídeo de entrada es SIEMPRE la grabación horizontal de OBS (no hay
  descarga de YouTube en este proyecto, a diferencia de newclips-viral-pipeline).
- Los cortes se aplican automáticamente sin cola de revisión manual.
- Silencio + acción visual (movimiento) NUNCA se corta, solo silencio + quietud.
- El zoom en `edit/` no es un micro-zoom en los puntos de corte, sino un zoom
  lento y continuo ("respiración de cámara") durante los tramos de habla
  continua largos (>= `long_speech_min_seconds`), derivados de la
  transcripción y remapeados a la línea de tiempo ya cortada.
- Se generan capítulos para la descripción de YouTube como subproducto.
- La intro sin cara (antes de que el usuario aparezca en `facecam_region`)
  se recorta siempre que se detecte con fiabilidad, sin exigir que además
  sea silencio — es la única señal de corte que no pasa por el filtro de
  movimiento.
- `facecam_region` vive en la raíz de la config (no dentro de `edit` ni de
  `detect_cuts`): lo usan ambos módulos.
- La miniatura (`thumbnail/`) se compone SIEMPRE a partir de frames reales
  del propio vídeo (cara + gameplay); Gemini solo puede mejorar el estilo
  de esa composición ya armada, nunca generar la imagen desde cero.
- La subida a YouTube (`publish/`) nunca sube nada de verdad salvo
  `execute=True` explícito, y sube siempre como `"private"` salvo que se
  pida explícitamente otra cosa — ambas son salvaguardas por defecto del
  parámetro, no una promesa de proceso.

## Próximo paso

**Primera ejecución real completa e integrada del pipeline, hecha
(2026-08-09):** `detect_cuts` (ya con `exclude_facecam_from_motion`
activo -- resuelve la nota pendiente que había aquí antes) ->
`edit` (smart cut + loudnorm + outro, sin zoom) -> `detect_chapters` ->
`subtitles` -> `thumbnail` (Pillow, sin Gemini) contra `dinoblade_1`
completo, sin cola de revisión manual entre etapas, tal como documenta la
arquitectura. Resultado: `final.mp4` de 4613.94s (146 cortes: 131
silencio + 14 muletilla + 1 intro, 1314.59s eliminados; + outro de
~10s), 12 capítulos, 632 subtítulos, miniatura compuesta -- **0
discontinuidades de PTS** verificado con `ffprobe` paquete a paquete
sobre el archivo completo (tras encontrar y corregir en el camino el bug
de `append_outro` documentado en la fila `edit`). Tiempo total de
proceso ~57min (detect_cuts 33m31s + edit 23m04s + detect_chapters 19s +
subtitles ~1s + thumbnail 15s). No se subió nada a YouTube (dry-run) ni
se hizo commit -- pendiente de revisión del usuario.

**Miniatura: dos rediseños sucesivos el mismo día (2026-08-09), después de
la ejecución de arriba, ambos a petición explícita del usuario tras
revisar el vídeo real:** primero se sustituyó el diseño original (paneles
con borde + titular con Claude + mejora con Gemini) por uno de "sujeto
recortado (`rembg`) sobre fondo desenfocado"; después, tras revisar
también ESE resultado, se simplificó drásticamente una vez más: el módulo
ya no compone NADA, solo extrae frames candidatos reales y el usuario
compone la miniatura a mano (ver fila `thumbnail` para el estado actual,
el único vigente en el código). Ambos rediseños dejan obsoleto el
bloqueo de `GEMINI_API_KEY`/facturación que había aquí originalmente:
Gemini no forma parte del módulo desde el primer rediseño. Los 5
candidatos reales de `dinoblade_1` generados con el diseño ACTUAL están
en `data/output/dinoblade_1/thumbnail_candidate_1.png` a `_5.png`,
enseñados al usuario para su revisión -- pendiente de que elija/componga
uno como `thumbnail.png` (paso manual, ya no lo hace ningún módulo) y de
su aprobación final antes de comitear.

**Bug de audio investigado y arreglado (2026-08-11), reportado en vídeos ya
publicados (dinoblade_1, icarus_1, shift_at_midnight_1,
how_many_dudes_1):** tartamudeo esporádico de sílaba ("cojones-nes") en
puntos internos del renderizado parcial sin pérdida de `edit/run.py`, no
en los puntos de corte de `detect_cuts` (esos ya ajustan el silencio a su
núcleo real, no eran la causa). Causa raíz: `_cut_segment_copy` copiaba
vídeo Y audio sin recodificar (`-c copy`) en `kf_start`/`kf_end`
-- válido para vídeo (son keyframes reales) pero no para audio (rejilla
temporal propia, independiente del GOP) ni, con B-frames activas, para el
límite superior del propio vídeo (goteo de frames del GOP siguiente).
Medido con vídeo sintético: ~33-47ms de audio duplicado en cada costura
cabeza→interior, y hasta ~130ms de vídeo de más en la costura
interior→cola. Fix en `_cut_segment_copy`: audio del interior
RECODIFICADO con seek preciso (barato); vídeo del interior en DOS pasadas
(copiar + remux con recuento exacto de frames -- combinar `-frames:v` en
una sola pasada con el `-ss`/`-to` original corrompía el contenido a
mitad de tramo, descartado tras verificarlo). Revalidado: 0.0ms de
solape, recuento de vídeo exacto, contenido bit-idéntico frame a frame.
Detalle completo (incluida una nota sobre cómo NO verificar bit-identidad
con `-ss` en archivos con B-frames, que dio falsos positivos/negativos
durante esta misma investigación) en el docstring de `src/edit/run.py`
("Solape de audio/vídeo en la costura..."). Test de regresión:
`tests/test_audio_seam_overlap.py` (nuevo) + `tests/test_smart_cut_segments.py`
(bit-identity check reescrito para no depender de `-ss`). No se ha tocado
ningún vídeo ya publicado -- el fix es solo para próximas ediciones.
Diagnóstico y fix confirmados por el usuario, comiteados.

Un bloqueo pendiente, no trabajo pendiente de implementación:

1. **Confirmación explícita para la primera subida real a YouTube:**
   `publish/youtube.py` (vídeo + miniatura + subtítulos) está verificado
   hasta construir la petición (`execute=False`); falta que el usuario
   confirme cuándo ejecutar la primera subida real (`execute=True`), y
   antes de eso probablemente configurar `YOUTUBE_CLIENT_SECRET_PATH` y
   completar el flujo OAuth por primera vez (aún no se ha hecho ni lo uno
   ni lo otro).
